<%= form_for @fat do |f| %>
  <% if @fat.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@fat.errors.count, "error") %> prohibited
      this fat from being saved:</h2>
    <ul>
    <% @fat.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
