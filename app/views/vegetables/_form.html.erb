<%= form_for @vegetable do |f| %>
  <% if @vegetable.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@fat.errors.count, "error") %> prohibited
      this vegetable from being saved:</h2>
    <ul>
    <% @vegetable.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
